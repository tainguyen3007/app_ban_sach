name: app_ban_sach
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter
  intl: ^0.17.0
  sqflite: ^2.3.0
  path: ^1.9.0
  shared_preferences: ^2.5.3
  firebase_core: ^2.30.0
  firebase_auth: ^4.17.0
  google_sign_in: ^6.2.1
  cloud_firestore: ^4.15.0
  uuid: ^4.0.0
  

dev_dependencies:
  flutter_launcher_icons: ^0.13.1
  flutter_test:
      sdk: flutter
  
  flutter_lints: ^5.0.0

flutter_launcher_icons:
  android: true
  ios: true
  image_path: "assets/logo_offical.png"

flutter:
  uses-material-design: true
  assets:
    - assets/
    - assets/default_images/